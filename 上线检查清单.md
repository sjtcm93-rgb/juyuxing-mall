# 橘与杏中医生活 · 上线检查清单（Phase 3）

> 适用阶段：MVP 开发完成、本地全流程跑通后，准备提审上线。
> 当前状态：Smoke 156/156、E2E 61/61 全绿；本地走 mock 支付可贯通「登录→加购→下单→支付→佣金→退款」。
> 本清单按执行顺序排列，**前一步完成再做下一步**。

---

## 0. 前置确认（现已具备，无需操作）

| 项 | 状态 |
|----|------|
| 小程序 AppID `wx6e685f787f1cd099` | ✅ 已写入 project.config.json |
| 云开发环境 `cloud1-d4gx1jxk675274501` | ✅ 已写入 app.js / cloudbaserc.json |
| 基础云函数（init/login/order/pay/payNotify…） | ✅ 已可部署 |
| 数据库集合（含 coupons/user_coupons/referral_events 等） | ✅ init 自动建 |
| 本地全流程联调 | ✅ Smoke 156 + E2E 61 通过 |

---

## 1. 微信支付商户接入（✅ 已完成 2026-07-12，控制台操作）

1. 登录 [mp.weixin.qq.com](https://mp.weixin.qq.com) → **微信支付** → 确认商户号 `1114186048` 已申请、已实名、可收款。
2. 进入**云开发控制台 → 云支付** → 绑定该商户号（按引导完成签约 / 证书上传）。
3. 确认小程序的**经营类目**与微信支付开通的类目一致（电商 / 美妆个护类）。

> ⚠️ 不绑定商户号则只能继续走 mock 支付，**无法真实收款**。这一步是上线硬前提。
> ✅ 用户已于 2026-07-12 完成商户绑定（pay.weixin.qq.com AppID 关联 + 后台开通）。下一步见第 2 节切真实支付。

---

## 2. 切换 mock → 真实支付（无需改数据库，重部署即可）

真实支付链路代码已就绪：`pay/index.js` 调 `cloud.cloudPay.unifiedOrder`，回调 `payNotify` 写订单 paid + 佣金 + 老带新奖励。

**关键改动**：`resolveUseMockPay()` 已改为「字段缺失 + 已配置商户号 → 自动走真实支付」。因为 `pay_config.default` 里已有 `subMchId = '1114186048'`（init 写入），且你已完成商户绑定（第 1 步），所以**不需要去数据库加 `useMockPay` 字段**，重部署 `pay` 函数后下一笔订单就会自动走真实微信支付。

操作步骤：
1. 微信开发者工具 → 左侧「云函数」→ 右键 **`pay`** → **上传并部署（云端安装依赖）**。
2. （可选）右键 **`payNotify`** → 同样上传并部署。
3. **真机预览**一笔小额（如 0.01 元）支付，确认：
   - 微信官方收银台正常拉起（不再是「秒成功」）；
   - 支付成功后订单状态变 `paid`；
   - `commissions` 集合新增一条佣金记录；
   - `payNotify` 云函数日志无报错。

> 回退方式：若需临时退回模拟支付，在 `pay_config.default` 文档新增 `useMockPay = true`（布尔值）即可；置 `false` 或删除该字段则恢复真实支付。

> ⚠️ 若真机支付报「商户号未关联 / 无权限 / 类目不符」等错误：说明云环境侧的支付签约还没完全生效，把报错原文发我，多半要在 pay.weixin.qq.com 补一步「产品开通 / 签约」。

> 📱 **支付只能在「真机预览」（手机微信扫码）下测试！** 在开发者工具的「模拟器」里点订单详情的「立即支付」，会触发真实 `wx.requestPayment`，而模拟器不支持拉起微信支付，会卡住或报不支持。代码已加超时兜底：模拟器下会自动弹「请使用真机支付」提示，不会卡死页面。下单→进订单详情→看订单信息这一步在模拟器里是正常的，只有"点支付"这一步必须在手机上做。

---

## 3. 商品真实图片替换（避免审核判「无实质内容」）

现状：`init` 创建的小紫瓶已配 `images: ['/images/product-xiaoziping.jpg']`，本地图片已存在（64KB）。可直用，也可后续替换为更高清的实拍图。

1. 准备商品实拍图：建议宽度 750px，WebP 或压缩 JPEG，**单张 ≤ 200KB**。
2. 二选一写入 `products.images`：
   - **方案 A（推荐）云存储**：上传到云存储拿到 `cloud://` fileID，直接写入 `images` 数组。
   - **方案 B CDN**：把 HTTPS 图片 URL 写入 `images`；同时要在小程序后台 **开发 → 开发设置 → downloadFile 合法域名** 中加入该 CDN 域名。
3. 至少补齐：首页主图、详情轮播图、购物车缩略图各 1 张。

> 审核会看页面是否「有实际可售商品」，无图或大量占位极易被打回。

---

## 4. 隐私合规（提审必过项，重要）

**现状**：`miniprogram/pages/user/user.js` 用 `wx.getUserProfile` 取昵称/头像，会触发微信隐私授权要求。

**必须做（否则审核打回或运行时弹系统隐私窗）**：

- **A. 填隐私保护指引（需你本人，控制台操作）**：小程序后台 → 设置 → 服务内容声明 → 用户隐私保护指引 → 勾选「收集昵称、头像」并提交。
- **B. 代码接隐私弹窗（✅ 已完成）**：已在 `app.js` 注册 `wx.onNeedPrivacyAuthorize` 全局拦截，新增品牌化 `privacy-popup` 组件挂到 `user` 页；`app.json` 已加 `__usePrivacyCheck__: true` 与 `requiredPrivateInfos: ["getUserProfile"]`。用户点登录触发 `getUserProfile` 时自动弹出授权框，同意后才真正拉起；拒绝则降级为仅 openId 登录。
- **C. 验证**：开发者工具中真机/模拟点击「我的」→ 登录，确认先弹隐私授权框、同意后正常拿到昵称头像。

**推荐（更稳、未来合规，可后续做）**：将登录改为 `wx.login` 仅取 openid；昵称/头像改用新版 `button open-type="chooseAvatar"` + `input type="nickname"` 组件采集，彻底规避隐私弹窗。MVP 先用 B 方案快速过审。

---

## 5. 类目与资质

- 电商类小程序须选**经营类目**（如「商家自营 - 美妆 / 个人护理」）。
- 部分类目需提交**资质**（营业执照、化妆品经营相关许可等）。
- 位置：小程序后台 → 设置 → 基本设置 → 服务类目。

---

## 6. 服务器域名（当前无需配置）

- 本项目全部走**云函数**（`wx.cloud.callFunction`），不经过 `wx.request` 外网调用，**无需**在小程序后台配置 request 合法域名。
- 若后续接入外部 API（如第三方物流查询），须在 **开发 → 开发管理 → 开发设置 → 服务器域名** 加 HTTPS 白名单。

---

## 7. 提审与发布

1. 微信开发者工具 → **上传代码**（建议勾选「上传时压缩」）。
2. 填写**版本号 + 项目备注**（如 `橘与杏 MVP v1.0`）。
3. 小程序后台 → 管理 → 版本管理 → 开发版 → **提交审核**。
4. 审核材料：
   - 功能页面截图（首页 / 商品详情 / 购物车 / 订单 / 我的 / 代理中心）；
   - 如需，提供**体验版**或测试账号；
   - 类目资质文件。
5. 审核周期通常 **1–7 天**；被拒按反馈修改后重新提交。

---

## 8. 上线后验证

- [ ] 真机走完整「下单 + 真实支付」闭环；
- [ ] 代理专属二维码注册 → 下单 → 佣金结算 → 提现审批链路；
- [ ] 退款申请 → 后台处理 → 状态回流到订单详情；
- [ ] 云函数日志（云开发控制台 → 日志）无异常报错。

---

## 附：常见被打回原因速查

| 原因 | 规避 |
|------|------|
| 隐私指引未填 / getUserProfile 未授权弹窗 | 见第 4 节 |
| 页面大量空白或占位图 | 见第 3 节，补真实商品图 |
| 类目不符或资质缺失 | 见第 5 节 |
| 无实际可售商品 / 虚假交易 | 确保商品、库存、价格真实 |
| 支付类目与经营类目不一致 | 商户号类目与小程序类目对齐 |
