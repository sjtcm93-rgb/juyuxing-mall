# 橘与杏商城上线检查清单

> 适用于 A/B/C 三端版本。详细说明见 [docs/ABC-DEPLOYMENT.md](docs/ABC-DEPLOYMENT.md)。

## 1. 本地验证

- [ ] `npm run smoke`
- [ ] `node scripts/e2e-mock.js`
- [ ] `node scripts/page-load-logic-test.js`
- [ ] `node scripts/admin-qr-auth-test.js`
- [ ] `npm run lint:functions`
- [ ] 微信开发者工具问题面板为 0

## 2. 数据库和迁移

- [ ] 已备份 CloudBase 数据库
- [ ] B 端“系统设置 → 迁移预检”的待迁移项已核对
- [ ] 先运行 A/B/C 迁移，再创建新增的唯一索引
- [ ] `users.referralCode`、`orders.orderNo`、库存预留、佣金、提现、退款、现金流水幂等索引均已创建
- [ ] 商品 `reservedStock` 和每个 SKU 的 `reservedStock` 已补齐

## 3. 云函数

- [ ] 已按 `cloudbaserc.json` 部署正式函数
- [ ] `maintenance` 每 5 分钟执行，且普通用户无法调用
- [ ] `payNotify` 只绑定微信支付平台回调，云函数权限拒绝小程序端和 Web 端直接调用
- [ ] 用真实回调验证 `openid`、`total_fee` 与订单用户、订单金额一致
- [ ] `adminQrAuth` 的自定义登录私钥、AppSecret 和权限规则已配置
- [ ] 正式发布时，`adminQrAuth` 与 `promotion` 均已设置 `MINIPROGRAM_ENV_VERSION=release`

## 4. 支付安全

- [ ] 云环境已绑定可用微信支付商户号
- [ ] `pay_config/default.subMchId` 与绑定商户号一致
- [ ] `pay_config/default.useMockPay` 已设为 `false`
- [ ] `pay` 云函数不存在 `ALLOW_MOCK_PAY=true`
- [ ] 缺少支付配置时，付款被明确拦截且订单仍为待支付
- [ ] 真机完成一笔小额真实支付，库存、订单、销售现金流、冻结佣金各只变化一次

如需临时测试模拟支付，必须同时开启数据库开关和 `pay` 云函数环境变量 `ALLOW_MOCK_PAY=true`；测试结束立即同时关闭。项目不会再因为缺少商户配置而自动模拟支付成功。

## 5. 三端权限

- [ ] A 端没有后台、店铺编辑、公开申请分销员入口
- [ ] C 端只能通过 7 天一次性邀请激活
- [ ] 分销员订单数据不包含客户姓名、电话或地址
- [ ] 运营不能退款或审核提现
- [ ] 财务不能修改商品或库存
- [ ] 只有店主能管理后台账号、运行迁移或清理测试交易

## 6. 售后和提现

- [ ] 未发货退款自动回库存
- [ ] 退货退款必须先确认收到退货，再选择是否回库存
- [ ] 重复退款处理不重复回库、冲销佣金或写现金流水
- [ ] 同一提现请求只生成一笔申请
- [ ] 退款造成负佣金时，余额恢复为正前不能提现
- [ ] 提现一期采用人工转账；财务实际打款后才点击“确认已打款”

## 7. 物流、合规与发布

- [ ] 发货保存物流公司、运单号和发货时间，A 端可查看
- [ ] 如承诺实时物流轨迹，已接入第三方服务并配置合法域名；否则只描述为发货信息查询
- [ ] 隐私保护指引、服务类目、经营资质和商品宣传文案符合审核要求
- [ ] 上传新版本并设为体验版，使用 1 名店主、1 名分销员、2 名消费者试运行
- [ ] 最后由商户负责人完成真实小额支付、退款和人工打款验收
