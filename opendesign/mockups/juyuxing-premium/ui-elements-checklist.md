# 橘与杏商城 UI 重设计 - 完整元素清单

> 基于项目当前的 24 个页面、11 个公共组件和完整交易/分销/管理流程梳理。
> 设计方向：现代高端洗化官方商城，以 Aesop / Le Labo 式天然实验室气质为参考，克制、专业、简约，不做可爱风。
> 当前核心视觉稿：`home.html`、`product.html`、`category.html`、`cart.html`、`user.html`。

---

## A. 全局框架

- 微信系统状态栏：时间、信号、电量，与页面背景自然衔接
- 顶部导航 TopBar：返回、页面标题、搜索、分享、更多，以及微信胶囊按钮避让区
- 底部 TabBar：仅 3 个主 Tab（首页 / 购物车 / 我的）；分类通过首页或搜索进入
- TabBar 状态：默认、激活、购物车数量角标/消息红点
- 安全区：底部 34rpx `safe-bottom` 留白(iPhone 底部指示条)
- 页面容器：标准左右边距、内容最大宽度、固定底栏占位、长页滚动区
- 装饰元素：草本线稿、配方编号、印章、细分隔线、英文 eyebrow，小面积使用

## B. 设计系统（沿用现有视觉稿）

- 主画布：暖米白 `#F5F1EB`；浅背景 `#FAF7F2`；柔和分区 `#EFE9DF`
- 表面：白 `#FFFFFF`；分割线 `#E8E2D8`；弱分割线 `#F0EBE2`
- 文字：主墨 `#1F1B16`；软墨 `#3A332B`；辅助 `#7A6F62`；弱文字 `#A89F92`
- 品牌色：松墨绿 `#2D4A3E`；鼠尾草绿 `#5C7868`；暖琥珀 `#B8845C`
- 交易强调：陶土红 `#C8553D`，只用于价格、主要购买 CTA、危险提示
- 语义：`#6B8E5A` 成功 / `#D89E4F` 警告 / `#C8553D` 危险 / `#5B7B9D` 信息
- 字体：Cormorant Garamond + Noto Serif SC（品牌/标题）；PingFang SC（正文）；SF Mono（订单号/物流号）
- 字重：正文 400，强调 500，避免大面积粗体
- 英文 eyebrow：全大写或小型大写，字距 `0.18em`，字号 18-22rpx
- 字号：xs 22 / sm 26 / md 30 / lg 34 / xl 40 / 2xl 48 / 3xl 56(rpx)
- 间距：8rpx 基础网格；页面边距 28-32rpx；区块间距至少 64rpx
- 圆角：卡片 12-20rpx；按钮 8-12rpx；只允许标签、头像、单选点使用 pill/圆形
- 边框：优先 1rpx 细线；阴影最大 `0 8px 24px rgba(42,38,34,.08)`
- 图片比例：商品主图 1:1；Banner 15:8 或 16:9；编辑内容图宽度统一
- 图标：统一线性图标，1.5-2rpx 描边；24/32/40rpx 三级尺寸
- 触控：可点击区域不小于 88×88rpx；禁用态保持可识别对比度

## C. 公共组件（11 个）

| 组件 | 模式 | 关键元素 |
|---|---|---|
| goods-card | grid / list | 封面图、角标(新品/热销/推荐)、品名、副标题、价格(`¥` + 主价 + 划线原价)、行内 + 按钮 |
| coupon-card | — | 左侧金额(`¥50` 或 `8折`)、使用门槛、有效期、右侧按钮(领取 / 已领取 / 已使用 / 已过期) |
| countdown | — | 天/时/分/秒，前缀/后缀文案，结束态 |
| spec-picker | 弹层 | 商品图+浮动价格、库存提示、已选、规格 chip(可选/禁用)、数量步进、底部双按钮 |
| price-tag | sm/md/lg + color 变体 | `¥` 符号 + 主值 + 划线原价 |
| status-tag | 7 种状态 | pending / paid / shipped / received / refunding / refunded / cancelled |
| empty-state | — | 图标、主文案、行动按钮(可选) |
| error-state | — | 图标、错误文案、重试按钮 |
| skeleton | card / list / banner / detail | 灰块占位 |
| region-picker | 弹层 | 三级 Tab(省/市/区) + 列表 + 取消/确定 |
| privacy-popup | 弹层 | 标题、正文(含隐私协议链接)、拒绝 / 同意按钮 |

此外需要建立以下基础组件：顶部导航、底部 TabBar、主/次/文字/危险按钮、搜索框、表单行、标签 chip、数字角标、分段控制、时间线、确认弹窗、Toast。

## D. 页面清单(24 个，按用户旅程分组)

### D1. 主流程(最常见 4 个，优先做)

**首页 `index`**
- 品牌 hero(大字 + 副标题 + 引言)
- 自动轮播 banner(标题/图)
- 热门单品区块标题 + 商品 grid
- 品牌故事卡片(引号 + 长文 + 跳转链接)
- 代理入口 banner
- 状态：骨架 / 错误 / 空 / 加载更多 / 到底

**分类 `category`**
- 左侧分类侧栏(滚动，激活态高亮 + 竖条指示)
- 右侧顶部 banner(当前分类名 + slogan)
- 排序条(综合 / 销量 / 价格)
- 商品 grid(2 列)
- 加载更多 / 到底

**商品详情 `product`**
- 主图轮播 + 指示器(可空态)
- 右上角收藏 ♥(空 / 实)
- 价格区(主价 + 划线原价 + 已售件数)
- 品名 + 副标题
- 已选规格行(展开 spec-picker)
- 商品详情 rich-text
- 品牌卡片
- 底部 sticky 操作栏：首页 / 购物车 / 客服(左) + 加入购物车 / 立即购买(右，双按钮)

**购物车 `cart`**
- 列表项：checkbox + 商品图 + 名称 + 规格 + 价格 + 数量步进 + 删除按钮
- 底部 sticky：全选 + 删除选中(左) / 合计 ¥xxx + 结算(N)(右)

### D2. 交易流程

**结算 `checkout`**
- 收货地址卡(姓名 + 电话 + 默认徽章 + 详细地址，空态"请选择")
- 商品清单(小图 + 名 + 规格 + 单价 + 数量)
- 优惠券行(已选 / 可选张数 › / 无可用)
- 订单备注 textarea
- 价格明细(总额 / 优惠 -¥ / 实付)
- 底部 sticky：合计 + 提交订单按钮(含提交中状态)

**订单列表 `order`**
- 顶部 Tab 切换(全部 / 待付 / 待发 / 待收 / 完成 / 售后，水平滚动)
- 订单卡：订单号 + 状态徽章 + 商品小行(图+名+规格+价) + 时间 + 合计 + 操作按钮(去支付 / 申请退款)
- 加载更多 / 到底

**订单详情 `order-detail`**
- 顶部状态条(状态徽章 + 描述)
- 收货地址卡
- 商品列表 + 价格明细
- 物流卡(跳转物流)
- 退款详情(若有)
- 订单信息(订单号+复制按钮 + 时间 + 备注)
- 时间线(订单进度，已完成/等待中两态)
- 底部 sticky：联系客服 + 按状态显示(取消+支付 / 申请退款 / 确认收货 / 申请售后)

**退款 `refund`**
- 模式切换：`detail`(只读) vs `submit`(提交)
- submit：订单摘要 + 退款金额 + 原因列表 chip(单选) + 退款说明 textarea + 提交按钮
- detail：状态徽章 + 申请时间 + 原因 + 描述 + 处理备注

**物流 `logistics`**
- 快递信息卡(公司 + 单号)
- 物流轨迹时间线(当前节点高亮 + 历史节点)

### D3. 用户中心

**个人中心 `user`**
- 头部：背景装饰 + 头像(未登录徽章) + 昵称 + 副标题(未登录提示 / 生活家)
- 订单状态 4 格：待付 / 待发 / 待收 / 售后 + 红点数字徽章
- 功能菜单卡：地址 / 收藏 / 优惠券 / 邀请有礼(带"送5元券"标签) / 客服 / 代理中心或成为代理(带"分享赚钱"标签) / 管理后台(仅管理员) / 关于
- 每行：图标 + 文字 + 可选红/橙 tag + 右箭头 ›

**地址列表 `address`**
- 列表卡：姓名+电话+默认徽章 / 地址详情 / 设为默认+编辑+删除(行内三操作)
- 空态(图标+文案+添加按钮)
- 底部 sticky 添加按钮

**地址编辑 `address-edit`**
- 表单：收货人 / 手机号(数字) / 省市区(触发 region-picker) / 详细地址 textarea / 设为默认 switch
- 底部 sticky 保存按钮

**收藏 `favorites`**
- 工具栏(共 N 件 + 管理/完成切换 + 全部移除)
- 商品 list(行模式，长按移除滑出)
- 加载更多 / 到底

**优惠券中心 `coupon-center`**
- 顶部：领券中心标题 + 我的优惠券 ›
- 优惠券卡列表(coupon-card 组件)

**我的优惠券 `coupon-mine`**
- 顶部 Tab：未使用 / 已使用 / 已过期
- 优惠券卡列表(带状态)

**邀请有礼 `referral`**
- 顶部大字标题 + 副文案
- 奖励规则卡(三步步骤号 1/2/3)
- 数据卡(已登录)：成功邀请数 + 获得奖励券数
- 行动区(已登录)：立即邀请(open-type share) + 复制链接 + 查券 ›
- 行动区(未登录)：登录提示 + 去登录
- 温馨提示列表

### D4. 探索

**搜索 `search`**
- 顶部固定：输入框(带搜索图标 + 清除 ×) + 取消
- 初始态：搜索历史(标签 chip，带 × 删除) + 热门搜索(标签 chip)
- 联想态：商品建议列表(搜索图标 + 名称)
- 结果态：排序条 + 商品 grid + 加载更多

**关于 `about`**
- 顶部：品牌字标(大字"橘与杏") + 副标语
- 品牌故事卡(引号 + 多段长文)
- 品牌理念(三个图标 + 名称 + 描述)
- 联系我们(客服微信 / 合作邮箱 / 版本号)

### D5. 代理 / 私域

**代理中心 `agent-center`**
- 顶部：头像 + 昵称 + 等级徽章
- 业绩面板(累计佣金 / 可提现 / 团队成员，3 列带分割线)
- 本月业绩卡(销售额 / 佣金 / 订单数)
- 提现管理卡(可提现金额 + 申请提现按钮)
- 我的推广卡(推广码 + 复制 / 推广链接 + 复制 / 分享小程序按钮 + 提示)
- 佣金记录列表

**代理申请 `agent-join`**
- 顶部 banner(图标 + 标题 + 描述)
- 代理权益卡(4 项：🔗 永久绑定 / 📈 阶梯返利 / 💰 快速结算 / 🎯 专属客服)
- 如何赚钱(三步步骤)
- 申请表单(姓名 / 手机号 / 申请理由 textarea)
- 底部 sticky 提交按钮

**提现 `withdrawal`**
- 余额卡(可提现金额大字 + 处理中提示)
- 表单：提现金额(`¥` 前缀 + 输入 + 全部) + 收款人 + 收款账号
- 提示文案(最低 10 元 / 1-3 工作日)
- 底部 sticky 提交按钮
- 提现记录列表(带状态)

### D6. 沟通 / 售后

**客服聊天 `chat`**
- 消息区(对方头像 + 气泡 + 时间 / 我方头像 + 气泡)
- 支持文本消息 + 图片消息
- 底部输入栏：输入框 + 发送按钮(空/有内容两态)
- 空态提示

### D7. 管理 / 商家（管理员可见）

**管理后台 `admin`**
- 顶部横向 Tab：概览 / 订单 / 代理 / 提现 / 退款 / 商品 / Banner / 客服
- Tab 数量角标：待发货、待审代理、待处理提现、待处理退款、客服未读
- 概览统计：总订单 / 待发货 / 销售额 / 待审代理 / 待处理提现 / 待处理退款
- 快捷入口：商城编辑器、发货、代理审核、提现处理、退款处理、商品、Banner、客服
- 订单工作区：状态筛选；订单号/状态/商品/金额；发货表单（物流公司、运单号、确认/取消）
- 代理工作区：待审核/已通过/已拒绝/全部；头像、姓名、联系方式、理由、状态；通过/拒绝
- 提现工作区：待处理/已打款/已拒绝/全部；金额、收款人、账号、时间；确认打款/拒绝
- 退款工作区：待处理/已同意/已拒绝/全部；订单号、原因、描述、商品、金额、时间、处理备注；同意/拒绝
- 商品工作区：新增；商品卡（名称/副标题/售价/库存/上下架）；编辑/上下架/删除
- 商品表单：名称、副标题、售价、原价、库存、规格、分类、图片、富文本描述、上下架状态、保存/取消
- Banner 工作区：新增；图片预览、标题、链接、排序、启用状态；编辑/启停/删除
- Banner 表单：图片链接或上传、跳转路径、标题、排序、状态、保存/取消
- 客服工作区：会话头像、昵称、最后消息、时间、未读数、空态

**自营小店 `shop-editor`**
- 顶部 Tab：商品（数量）/ 轮播图（数量）
- 商品工具栏：搜索、状态筛选、新建商品
- 多选批量栏：批量上架、批量下架、复制、改分类、取消
- 商品卡：多选框、主图、名称、上下架、草稿、排序、分类、副标题、价格/原价、库存、销量
- 商品单项操作：复制、排序、发布；长按进入多选
- 商品编辑弹层：名称、副标题、售价/原价、库存、分类、动态规格行
- 商品图管理：最多 9 张、上传、删除、左右排序、封面顺序
- 富文本编辑器：粗体、斜体、标题、有序/无序列表、插图、清空
- 状态单选：上架 / 下架；底部取消 / 创建或保存
- Banner 工具栏：新增、保存排序、拖动提示
- Banner 卡：拖动手柄、图片、标题、链接、排序、启停状态、编辑/启停/删除
- Banner 编辑弹层：上传/替换/移除、标题、跳转路径、排序、状态、取消/保存

## E. 状态 / 反馈元素

- 加载中：三圆点呼吸 / spinner
- 加载更多：小 spinner + 文字
- 已经到底：- 文字分隔
- 空态：图标 + 文案 + 可选行动按钮(由 empty-state 组件)
- 错误：图标 + 文案 + 重试按钮(由 error-state 组件)
- 骨架：卡片 / 列表 / banner / 详情四种变体
- 调试状态条(开发态，上线前移除)
- 角标 / 红点(订单 Tab 计数、商品卡徽章)
- Toast 提示(成功 / 失败 / 警告)
- 模态弹窗(隐私 / 规格 / 地区 / 删除确认 / 取消订单 / 确认收货)
- 按钮状态：默认 / 按下 / 禁用 / 加载中 / 成功完成
- 图片状态：加载中 / 加载失败 / 默认占位 / 已删除
- 网络状态：离线提示 / 请求超时 / 重试
- 库存状态：正常 / 低库存 / 售罄 / 商品下架
- 权限状态：未登录 / 普通用户 / 代理 / 管理员

## F. 表单元素

- text input(单行，支持 placeholder / 清除 / 数字键盘 / 文本键盘)
- textarea(多行，字数限制)
- switch(开关，带颜色变量)
- checkbox(圆 / 方，选中 + 文字)
- radio / 单选 chip(reason-list 使用)
- stepper(数量步进 - N +)
- picker 触发器(规格 / 地区，带 › 箭头)
- switch-tab(横向滚动 + 激活态)
- 复制按钮(单号、推广码、链接)
- 搜索框（前置搜索图标、输入、清空、取消、联想）
- 图片上传（空态、上传中、成功、失败、预览、替换、移除、排序）
- 富文本编辑器（格式工具栏、编辑区、插图、清空）
- 表单校验（必填星号、行内错误、字数统计、提交失败定位）

## G. 弹层 / 浮层

- 隐私授权弹窗(标题 + 长文 + 协议链接 + 拒绝 / 同意)
- 规格选择抽屉(sp-panel：商品头 + 规格 chip + 数量 + 双 CTA)
- 地区选择器(rp-panel：三级 Tab + 滚动列表 + 取消 / 确定)
- ActionSheet / 操作菜单(如分享渠道)
- Toast / 轻提示
- 商品/Banner 编辑全屏弹层
- 优惠券选择底部抽屉
- 二次确认对话框：删除、清空、取消订单、确认收货、拒绝审核
- 图片预览层与上传进度层

---

## H. 设计交付时必须覆盖的状态

每个页面不能只画一个“有数据”的静态画面，至少检查以下状态：

1. 默认有数据
2. 首次加载骨架
3. 空数据
4. 网络错误与重试
5. 下拉刷新/加载更多/已经到底
6. 未登录与已登录
7. 按钮禁用与提交中
8. 超长商品名、超长地址、超长订单号
9. 低库存、售罄、下架
10. iPhone 安全区和微信胶囊避让

## I. 推荐设计顺序

**P0：先完成可购买闭环**

首页 → 分类/搜索 → 商品详情/规格选择 → 购物车 → 确认订单 → 支付结果/订单详情 → 地址选择。

**P1：再完成复购与服务**

个人中心 → 订单列表 → 物流 → 退款售后 → 收藏 → 优惠券 → 客服 → 关于品牌。

**P2：最后完成增长与经营**

邀请有礼 → 代理申请 → 代理中心 → 提现 → 管理后台 → 商城编辑器。

---

## J. 高端简约风执行原则

按"高端低调 / Aesop 风"已确立的方向，每个新页面都应保持：

1. 留白大于装饰：区块之间至少 64rpx 呼吸，不用密集卡片堆叠。
2. 商品是主角：首屏必须看到真实产品、包装、质地或使用方式。
3. 标题使用中英衬线组合，正文用清晰无衬线；英文 eyebrow 保持统一字距。
4. 墨绿承担品牌与主操作；陶土红只承担价格、购买和危险语义；暖琥珀只点缀。
5. 卡片 12-20rpx、按钮 8-12rpx，避免大圆角、胶囊按钮和“可爱”图标语言。
6. 以细边框和材质色块建立层级，阴影只用于固定底栏、弹层和少量悬浮元素。
7. 一屏一个视觉重心，装饰只使用印章、编号、草本线稿、英文小标题中的少数几种。
8. 动效克制：页面入场、图片切换、抽屉上滑、收藏反馈即可，时长 180-320ms。

> 这份清单可直接作为 Figma 页面树、组件库清单和验收 checklist 使用。
