# 橘与杏商城 项目交接报告

> 交接日期：2026-09-14
> 项目路径：`/Users/orange/Documents/橘与杏商城/`
> 代码版本：main 分支 c0846cb（商家转账升级版接口 + 防重复转账保护）
> 小程序已上传版本：1.1.2（需在 mp 后台确认是否已设为体验/线上版）

---

## 一、项目概况

「橘与杏」中医生活日化品牌的自营电商微信小程序，采用**品牌直营 + 单级分销**模式：

- **C 端（消费者）**：浏览商品、加购、下单、微信支付、查物流、退款售后
- **B 端（店主/运营/财务）**：网页版管理后台，负责订单发货、库存、商品、退款审批、分销员管理、提现审批
- **C 端（代理/分销员）**：小程序内分销中心，专属二维码推广、佣金查看、提现申请

佣金模式：单级分销，固定可调比例（存于数据库 `admin_config`，当前默认 **33%**），订单确认收货后佣金解冻可提现。

---

## 二、账号与核心资源清单

| 资源 | 标识 | 用途 | 持有人 |
|---|---|---|---|
| 小程序 AppID | `wx6e685f787f1cd099` | 三端共用 | 店主微信账号 |
| 云开发环境 ID | `cloud1-d4gx1jxk675274501`（上海区） | 全部后端 | 店主 |
| 微信支付商户号 | `1114186048` | 收款、退款、转账 | 店主 |
| 商户 API 证书 | `cloudfunctions/admin/certs/apiclient_key.pem`（已 gitignore） | 商家转账签名 | 服务器/本地 |
| 商户证书序列号 | `557EC5ABB4E20C594FAAB3AD48E279F360B6887B` | — | — |
| B 端后台 | 静态托管于云开发（admin-web） | 管理后台 | — |
| 数据库备份 | `backups/`（gitignored，含敏感信息，**勿外传**） | 灾备 | 本机 |

⚠️ 两个后台入口要分清：

- **微信内置云开发控制台**：mp.weixin.qq.com → 云开发（或微信开发者工具内）
- **腾讯云 CloudBase 控制台**：tcb.cloud.tencent.com（功能更全，套餐管理在这里）

---

## 三、技术架构

### 3.1 三端结构

```
├── miniprogram/            # A 端主包（消费者商城）+ 全部页面
│   ├── pages/              # 首页/分类/商品/购物车/订单/售后/地址 等 27 个页面
│   └── subpackages/
│       ├── distributor/    # C 端代理分包（分销中心、提现）
│       └── admin-auth/     # B 端扫码登录确认页（中转用）
├── admin-web/              # B 端管理后台（静态页，托管在云开发静态托管）
├── cloudfunctions/         # 云函数（后端）
├── scripts/                # 测试与运维脚本
├── docs/                   # 详细文档（ABC-DEPLOYMENT.md 为主部署手册）
├── cloudbaserc.json        # 函数部署清单与超时/内存配置
└── database-indexes.json   # 幂等唯一索引定义
```

### 3.2 云函数清单（正式部署 19 个，以 cloudbaserc.json 为准）

| 函数 | 超时 | 职责 |
|---|---|---|
| login / product / banner / category / favorite / cart | 5s | 基础商城 |
| coupon / chat / order / pay / agent / commission / withdrawal | 10s | 交易与分销 |
| payNotify | 20s | 微信支付回调（只允许平台调用） |
| admin / adminQrAuth / promotion | 20s | 后台 / 扫码登录票据 / 推广码 |
| maintenance | 30s | 定时任务（每 5 分钟：超时订单释放库存、7 天自动收货） |
| refund-processor | 60s | 退款兜底轮询 |

另有 4 个**非正式**函数：`init`（初始化）、`updateProductDesc` / `uploadProductImage`（维护工具）、`admin-refund-http`（**已退役**，返回 410，勿再使用）。

### 3.3 B 端权限体系

- 账号存数据库（`admin_accounts`），三角色：**admin_owner**（全权+管账号）、**admin_operator**（不能审提现）、**admin_finance**（不能改商品库存）
- 登录方式：B 端页显示二维码 → 店主微信扫码 → 小程序确认 → 2 分钟有效票据登录
- ⚠️ 旧的 `admin_config.adminOpenIds` 白名单登录方式**已废弃**

---

## 四、核心业务与资金流

### 4.1 购物主流程

下单锁定库存（reservedStock）→ 微信支付 → payNotify 回调核验（openid + 金额）→ 正式扣库存、记现金流水、佣金入 frozen → 发货 → 确认收货（或 7 天自动）→ 佣金 frozen → settled。

30 分钟未支付由 maintenance 释放库存。支付对账有自愈逻辑（卡单自动修复）。

### 4.2 退款

买家申请 → B 端审批同意即自动执行（cloudPay 服务商模式 + AppSecret 自触发链路）→ 微信 SUCCESS 后事务同步。失败单据转「待人工核查」，B 端有「重试退款」按钮（微信按 outRefundNo 幂等，不会重复退）。

⚠️ **新商户常见坑**：收款次日结算到银行卡，商户基本账户没钱会拒退。需在商户平台充值后重试。

### 4.3 提现 ⚠️ 当前为半自动（重点交接项）

**设计目标（代码已就绪）**：代理小程序申请 → B 端审批 → 自动调微信商家转账 API（升级版 transfer-bills，1005 佣金场景）→ 代理微信确认收款 → 完成。防重复转账、单笔 ≥0.3 元均已实现。

**当前实际状态：自动打款被套餐卡住，暂走人工模式**：

1. 代理在小程序申请提现 → B 端审批通过（单据停在 pending_pay「待打款」）
2. 店主登录 pay.weixin.qq.com → 产品中心 → 商家转账 → **手动发起转账**（扫脸确认）
3. 回 B 端点「确认已打款」

**卡点根因（2026-09-14 定性）**：
- 微信要求调转账 API 必须配固定出口 IP 白名单
- 云开发环境当前为**免费体验版**套餐，官方明确固定公网出口 IP 仅「标准版及以上」支持（¥179.10/月起）
- 已实测验证：升级套餐后用 CLI 一条命令即可开启 EIP（路径已跑通），无需网页操作

**升级套餐后恢复自动转账的步骤**（未来操作）：
1. tcb.cloud.tencent.com → 套餐用量 → 升级标准版
2. CLI：`tcb api scf UpdateFunctionConfiguration --api-version 2018-04-16 --body '{"FunctionName":"admin","Namespace":"cloud1-d4gx1jxk675274501","PublicNetConfig":{"PublicNetStatus":"ENABLE","EipConfig":{"EipStatus":"ENABLE"}}}'`
3. 商户平台 → 安全能力 → 接口 IP → 把分配的 EIP 加白名单
4. 商户平台**运营账户充值**（转账资金从运营账户出，与基本账户分开）
5. B 端对 pending_pay 单据重试即可，代码无需改动

**遗留测试单**：提现单 `853d22036aa78a59030067660aaf455b`（¥1 测试，停在 pending_pay），建议直接 B 端驳回。

### 4.4 免费套餐的其他隐患

体验版套餐下云函数**超时固定 3 秒、内存固定 256M**（cloudbaserc 里配的 5-60s 超时实际不生效或被部分生效——目前未触发线上故障，但 admin 长链路操作有超时风险，遇函数莫名失败先想到这条）。

---

## 五、当前状态总结

### 已完成并实测验证 ✅

- 三端架构全部跑通，B 端三角色权限隔离
- 真实微信支付开通并验证（payNotify 回调修复，2026-09-13）
- 退款端到端实测到微信真实响应
- 佣金、冻结/解冻、提现规则（无最低金额，>0 即可）
- 207 项 smoke 检查全绿；e2e-mock 57 项全链路验证
- 商家转账升级版 API 代码全部就绪（commit c0846cb）

### 待办事项 📋

1. **小程序提审上线**：1.1.2 已上传，正式发布前按 `上线检查清单.md` 全项过一遍
2. **正式商品图片替换**：当前仍是占位图
3. **替换/清理**：订单页调试状态条移除；云端 `zz-dbinspect` 临时函数删除（已桩化）
4. **提现测试单** `853d22036aa78a59030067660aaf455b` 驳回处理
5. （可选）升级标准版套餐 → 开自动转账（见 4.3）
6. （可选）运营账户充值——手动转账也从商户平台出，确认账户余额

### 规划中（阶段四）

代理等级、拼团、会员等级、企业微信私域运营。参考《私域增长与分销落地规划.md》。

---

## 六、重要技术坑档案（踩过的坑，接手必读）

按严重程度排列：

1. **云函数事务禁并发**：`db.runTransaction` 内严禁 `Promise.all`——事务会话不支持并发，报 TransactionBusy。提现功能曾因此上线即失败。事务内一律逐个 await。
2. **CLI 部署重置超时**：`cli cloud functions deploy` 会把函数超时重置为默认 3 秒（两次真机事故）。每次 CLI 部署后必须立即补设超时（标准值见 cloudbaserc.json）。**优先用 MCP updateFunctionCode 部署**（不重置配置，但有 1-3 分钟传播延迟）。
3. **wx-server-sdk 嵌套对象坑**：update 嵌套对象会被展平成点路径，null 字段上建子字段直接报错。整体替换用 `_.set()`；文档不要写 `字段: null` 占位。
4. **payNotify 回调身份口径**：云开发服务商模式回调，`event.openid` 是平台侧标识，**`event.subOpenid` 才是小程序用户**。写错则真实支付回调永远失败。
5. **退款必须带小程序票据**：B 端网页/直调没有票据（-501001）。解法：AppSecret 换 stable_token → `api.weixin.qq.com/tcb/invokecloudfunction` 自触发（SOURCE=wx_http 上下文 cloudPay 可用）。AppSecret 已配在 admin 云函数环境变量 `MINIPROGRAM_APPSECRET`。
6. **B 端静态资源缓存**：每次改 admin-web 必须升 index.html 版本号，否则浏览器缓存旧 JS 导致白屏。
7. **支付 mock 双开关**：模拟支付必须同时开数据库 `pay_config.useMockPay=true` + pay 函数环境变量 `ALLOW_MOCK_PAY=true`，任一缺失不生效。上线时两者必须都为 false。
8. **部署传播延迟**：云函数部署后立即调用可能命中旧热实例，等 1-2 分钟再验证。

---

## 七、日常操作速查

### 本地验证（提交前必跑）

```bash
npm install
npm run smoke                          # 207 项静态检查
node scripts/e2e-mock.js               # 57 项端到端（需先把 mock-agent-A 设为管理员）
npm run lint:functions
```

### 部署

详见 `docs/ABC-DEPLOYMENT.md`（权威手册）。要点：

- 云函数以 `cloudbaserc.json` 为准；优先用 MCP 部署，CLI 部署后必须补超时配置
- B 端：admin-web 为静态托管，改动后升版本号再上传
- 小程序：微信开发者工具上传，用户需在 mp 后台选为体验版/提交审核

### B 端后台登录

浏览器打开 B 端地址 → 页面出二维码 → 店主（或对应角色）微信扫码 → 手机上确认 → 自动登录。管理员账号在数据库 `admin_accounts`，只由 admin_owner 在「账号管理」里维护。

### 数据库备份

备份文件存 `backups/`（含支付配置敏感信息，gitignored）。当前最新：`db-backup-2026-09-13T09-12-44-873Z.json`（135 文档）。重大操作前先备份。

### 微信开发者工具 CLI（自动化）

`/Applications/wechatwebdevtools.app/Contents/MacOS/cli`（需 IDE 运行中）。可脚本化部署函数、上传代码。配合 miniprogram-automator（ws://localhost:9420）可自动化测试。

---

## 八、文档索引

| 文档 | 内容 |
|---|---|
| `docs/ABC-DEPLOYMENT.md` | **主部署手册**（最权威） |
| `上线检查清单.md` | 上线前逐项检查 |
| `DEPLOY.md` | 部署入口 + 交易安全要求 |
| `私域增长与分销落地规划.md` | 阶段四业务规划 |
| `scripts/smoke.md` | 测试说明 |
| `docs/plans/*.md` | 历次方案设计文档 |
| `.workbuddy/memory/` | 开发过程日志（坑与决策全记录） |

---

## 九、交接确认建议

接手人建议按此顺序熟悉项目：

1. 通读本报告 + `docs/ABC-DEPLOYMENT.md`
2. 本地 `npm run smoke` 跑通，确认环境正常
3. B 端扫码登录一次，熟悉订单/商品/退款/提现各页面
4. 小程序体验版完整下一单（真实支付小额）+ 申请一次退款，观察全链路
5. 遇到问题先查 `.workbuddy/memory/` 里的坑档案，90% 的坑都有记录

---

*报告生成：2026-09-14，基于项目仓库 main 分支 c0846cb 与开发日志整理。*
