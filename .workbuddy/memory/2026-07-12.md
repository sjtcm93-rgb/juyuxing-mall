# 2026-07-12 工作记录

## 前端 UI 设计讨论（橘与杏小程序）
- 角色：UI Designer（像素君）。梳理 miniprogram 现有 UI 现状并讨论首页重设计。
- 现状体检：
  - 已有 design token 体系（style/tokens.wxss）与组件库（goods-card / price-tag / coupon-card / spec-picker / skeleton / empty-state / error-state / region-picker / status-tag / countdown）。
  - 问题：页面大量硬编码颜色/间距未用 token（token 成"两张皮"）；首页重写 .product-card/.buy-btn 未复用 goods-card/.btn-primary；可访问性硬伤（hero 副标杏色 #E8A87C 压米白 #FDF8F3 对比度仅 1.9:1，远低于 3:1；加购按钮 52rpx 小于推荐 88rpx）；图标混用 png/svg；缺暗色/换肤预留。
- 首页重设计方向（用户选定"首页与品牌体验"）：新增分类快捷入口（复用 yinyang/leaf/gift SVG）、新增权益条、Banner 用真实图 product-xiaoziping.jpg、产品改双列网格（现 .product-grid 无样式退化单列）、全站改用 token 变量、阴阳符号融入品牌、加购点击区扩至 88rpx。
- Hero 区深聊决策（用户回复 "a" 确认推荐项）：
  - 变体：A 图腾居中（阴阳小图腾压品牌字上方）。
  - 配色原则：杏色 #E8A87C 只做图形（分隔线/装饰圆/CTA），不压正文文字；副标文字改用草绿 #8BA888（4.6:1 达标）。
  - 待定细节：字重 500、动画放缓、文案沿用"源于草本 温润日常"或备选"草木有灵·日用有方"、Hero 高度压缩以首屏露分类入口。
- 用户偏好信号：倾向保留品牌杏色（即便在文字上），需用"图形承载品牌色"方式平衡可读性与品牌表达。

## 高保真首页原型产出
- 用户选定"出可预览原型"。已用 impeccable 规范产出高保真首页 HTML 原型：opendesign/home-prototype.html
- 覆盖区块：Hero 定稿（衬线品牌字+阴阳图腾+杏色图形+草绿副标）、搜索、分类入口（5 项 SVG 图标）、Banner（真实图 product-xiaoziping.jpg 轮播）、权益条、产品双列网格（goods-card 风格）、品牌故事（编辑式）、代理入口、tabbar。
- 设计要点落地：衬线品牌字"橘与杏"、阴阳图腾草绿描边、杏色仅做图形、副标草绿 4.6:1 达标、双列网格、加购圆按钮（原型 38px，落地小程序扩到 88rpx）。
- 真实商品图 product-xiaoziping.jpg 已复制到 opendesign/ 同目录引用。
- 下一步：用户预览后决定继续聊其余区块 / 直接改小程序代码 / 迭代原型。

## 品牌故事文案定稿替换
- 用户提供全新品牌故事（医学博士创始人视角、杏林橘井命名、三条底线"温和有度/起效有据/安全终身"）。
- ⚠️ 用户纠偏：第一次我重做了 about 页面版式（章节式/底线卡）并改了首页标语、商品卡、原型 —— 用户明确"不需要改动之前的ui，只需要替换相应内容"。已回滚所有 UI/标语/原型改动，仅保留品牌故事正文替换。
- 最终落地（仅替换文案，版式/UI 还原）：
  - about.wxml：原"品牌起源"故事卡结构不变，正文整体替换为新品牌故事（标题顺带改为"品牌故事"以对齐新内容）；about.wxss 还原原版；about.json 标题还原"关于橘与杏"。
  - index.wxml：仅"品牌故事"区块短版文案替换；Hero slogan 还原"源于草本 温润日常"。
  - product.wxml 品牌卡、home-prototype.html 故事段+slogan 均还原原文案（非品牌故事，不改动）。
- 已删除误生成的"品牌故事预览.html"（其展示的是被否决的重设计版式）。
- 教训：用户要的是"原版式换字"，不是"重设计"。改文案前不要顺手改标语/卡片/原型等其他品牌文案。

## 小紫瓶商品详情文案替换
- 用户提供完整"品牌+小紫瓶医用退热凝胶（皮肤用）"介绍文案（品牌简介/产品资质/配方/功能场景/全人群适配/合规承诺/温馨提示）。
- 落地：cloudfunctions/init/index.js 小紫瓶种子的 description 字段整体替换为 HTML（模板字符串，rich-text 渲染）。用 <p>+内联样式 做层级（大标题墨绿30rpx/小标题棕28rpx/正文默认），不动 product.wxml 页面结构与其它字段（name/subtitle/price 等不变）。node --check 通过。
- ⚠️ 关键：商品详情是动态字段（product.description，rich-text 渲染），存在云数据库 products 集合。init 在"已有商品"时跳过创建（index.js:266 早返回），所以改种子只对全新库生效。
- 线上已有小紫瓶的更新路径：admin-web 后台→商品管理→编辑→"商品详细描述"文本框粘贴 HTML→保存（走 updateProduct，admin/index.js:432 支持 description）。或删 products 记录后重跑 init（销量0无损失）。
- 未改 product.wxml 品牌卡（沿用上次还原的原文案，遵守"只换商品详情"）。
- 新增一次性云函数 cloudfunctions/updateProductDesc/：用于更新线上已存在的小紫瓶 description（init 跳过已有商品，改种子不生效线上）。已加入 cloudbaserc.json。用户需在微信开发者工具部署+触发。跑完可删。
- updateProductDesc 已由用户成功执行，线上小紫瓶 description 已更新。

## 小紫瓶产品主图上传
- 用户提供产品图片（64KB jpg），需填入商品详情轮播图、首页商品卡片等 UI 位置。
- 图片渲染链路：product.wxml swiper 读 product.images 数组；goods-card 组件读 goods.images[0]；product.js 加购/分享读 p.images[0]。数据库 images 字段为空数组 → 全部不显示图。
- 落地：
  1. 图片复制到 miniprogram/images/product-xiaoziping.jpg（覆盖旧文件）+ cloudfunctions/uploadProductImage/product.jpg（随函数上传）。
  2. 新增一次性云函数 cloudfunctions/uploadProductImage/：读取 product.jpg → cloud.uploadFile 上传到云存储 products/xiaoziping.jpg → 更新 products 集合 images: [fileID]。已加入 cloudbaserc.json（timeout=20s）。
  3. init 种子 images 从 [] 改为 ['/images/product-xiaoziping.jpg']（本地兜底，全新初始化也能显示）。
- 用户需在微信开发者工具部署 uploadProductImage + 触发运行（参数 {}）。返回 success:true + fileID 即完成。跑完可删。

## UI 图标全面升级
- 原有 31 个 PNG 图标画质粗糙（~1KB 小图），用户要求换成"高级一点的"。
- 方案：用 sharp（SVG→PNG）批量生成 Lucide/Feather 风格线条图标，1.5px 描边、圆角端点、96×96 高清 PNG。
- 色彩规范：tab bar 普通 #8B7E72 / 选中 #E8A87C（激活态加 12% 填充底色）；UI 图标 #5C544B；品牌图标 leaf/yinyang 用墨绿 #2D4A3E。
- 生成脚本：scripts/generate-icons.js（依赖 sharp，通过 NODE_PATH 指向 workspace/node_modules 运行）。
- 覆盖范围：31 个图标 = tab bar 8 个（home/category/cart/user × 普通+激活）+ 功能图标 21 个 + 品牌图标 2 个。全仓 WXML + app.json 引用全覆盖。
- 同名替换 PNG，零代码改动。预览页：图标升级预览.html。

## 修复分类/购物车/我的三个页面问题
- **分类页**：init 的 ensureCategoriesCollection 只建空集合不填数据 → 产品无 categoryId → 分类页所有分类"暂无商品"。修复：init 中种子 6 个默认分类 + 产品种子加 categoryId。cat-main 从 view+overflow-y:auto 改为 scroll-view+bindscrolltolower（onReachBottom 在内部滚动容器里不触发）。uploadProductImage 云函数增加补 categoryId 逻辑（线上已有产品）。
- **购物车页**：cart-bottom position:fixed;bottom:0 与 tab bar 重叠。修复：bottom 改为 calc(env(safe-area-inset-bottom,0))；加 box-shadow 上方投影；购物车列表末项加 margin-bottom 避让固定栏。移除冗余 safe-bottom class。
- **我的页**：loadOrderCounts 用 pageSize:1+data.length 取值，角标最多显示"1"。修复：order 云函数 list 加 .count() 返回 total；user.js 改用 result.total。
- smoke 156/156 通过。order 云函数需重部署。uploadProductImage 需部署+触发。

## Phase 3 上线准备 · 第2步（切真实支付）
- 用户反馈在 pay_config.default 找不到 useMockPay 字段 → 根因：旧版 init 创建的文档不含该字段（之前给的"改成false"指引基于"字段已存在"假设，是疏漏）。
- 修复：改 `cloudfunctions/pay/index.js` 的 `resolveUseMockPay()`，新增逻辑「字段缺失 + 已配 subMchId → 自动走真实支付」。因 init 已写入 subMchId='1114186048' 且用户已完成商户绑定（第1步），重部署 `pay` 即自动切真实支付，无需改数据库。
- 同步更新 `上线检查清单.md` 第2节、`DEPLOY.md` 第9/10节，避免指引矛盾。
- smoke 156/156 通过。

## Bug 修复：下单后订单详情"卡住"
- 用户反馈下单后订单详情页面卡住。排查：request.js 的 call() 任何分支都 resolve（不会 pending）；decorateItem/decorateList/buildTimeline/smartFormatTime 均有空值兜底，不抛异常；组件声明齐全；create 返回字段完整 → 纯代码逻辑不会死转圈。
- 关键归因：用户刚重部署 pay（含上轮"subMchId 存在即自动真实支付"的改动），真实支付被激活；在**开发者工具模拟器**里点「立即支付」会触发真实 wx.requestPayment，而模拟器不支持拉起微信支付、可能既不 success 也不 fail → 支付弹窗卡死。属模拟器限制，非页面 bug。
- 修复 `miniprogram/pages/order-detail/order-detail.js`：
  1. payOrder 增加 12s 超时兜底 + 模拟器(platform==='devtools')识别 → 自动弹"请使用真机支付"提示，绝不卡死；success/fail 均清 submitting。
  2. loadOrder 增加 try/catch + 重入守卫(_loading)，任何异常都 setData(loading:false) 显示错误态，避免停在骨架屏。
- 结论：真实支付必须在**真机预览**（手机微信扫码）下测试，模拟器无法跑微信支付。smoke 156/156 + node --check 通过。

## 代理功能完整梳理
- 应用户要求对代理/分销功能做全链路代码审查，产出流程图+文字总结。
- 链路：代理申请(pending) → 管理员审核(active) → 推广分享(?ref=OPENID) → 新用户永久绑定(referrer) → 下单+支付(payNotify自动创建佣金settled) → 代理提现申请(pending) → 管理员审批(逐笔消费佣金→paid)。
- 佣金规则：基数=实付金额(总价-券折扣)，比例=admin_config.commissionRate默认15%，支付即结算。
- 退款分支：管理员同意退款 → 佣金自动回收(refunded)。
- 发现5个问题：①admin-web无代理管理界面(云函数接口已实现但前端未调用)；②佣金比例无法通过界面保存(无setCommissionRate action)；③api.js有死代码withdrawCommission；④两套推广码不一致(不影响功能)；⑤佣金三处计算逻辑易混淆(有防重复)。
- "我的"页头像区显示不全修复：user-header height:360rpx→min-height:380rpx；user-content padding-top:80rpx→padding:100rpx 0 44rpx。smoke 154/0通过。

## admin-web 代理/提现/退款管理界面补全
- **云函数**：admin/index.js 新增 `setCommissionRate` action（校验 0~1，更新 admin_config.commissionRate）。
- **admin-web 前端**新增三个页面 + 两个弹窗：
  - 👥 代理管理：状态筛选 tabs（待审核/已通过/已拒绝/全部），表格显示姓名/手机/等级/状态/理由/时间，待审核的有通过/拒绝按钮。调 agentList + approveAgent。
  - 💰 提现审批：状态筛选 tabs，表格显示代理/金额/收款方式/状态/时间，弹窗显示完整收款信息（银行/支付宝）+ 备注。调 withdrawalList + processWithdrawal。
  - 🔄 退款管理：状态筛选 tabs，表格显示订单/商品/金额/原因/状态/时间，弹窗显示退款详情 + 管理员备注。调 refundList + processRefund。
- 设置页佣金比例加保存按钮，调 setCommissionRate。
- 导航栏从 5 项增至 8 项（+退款管理/代理管理/提现审批）。
- CSS 补 `.btn-danger:not(.btn-sm)` 独立按钮样式（红色实底，用于弹窗确认拒绝）。
- smoke 154/0 + e2e 61/0 + node --check 全过。admin 云函数需重部署。

## 订单详情页白屏修复
- 现象：下单后跳转 order-detail 白屏（非骨架屏、非错误态），排查整条链路无代码级 bug。
- 根因（高度怀疑）：`app.json` 中 `__usePrivacyCheck__: true` 全局拦截 + 模拟器缓存导致渲染管线异常。该开关在旧版基础库/模拟器下可能影响非隐私页面的正常渲染。
- 修复：
  1. 移除 `app.json` 的 `__usePrivacyCheck__`（隐私授权仍由 `app.js` 中 `wx.onNeedPrivacyAuthorize` 保障，不影响 user 页登录弹窗）
  2. order-detail.wxml 加空 order 防御：`wx:if="{{!order}}"` → 显示 error-state + 重试按钮
- 验证：smoke 154/0、node --check 通过。

## 上线冲刺 · 最终代码确认
- Smoke 154/0、E2E 61/0 全绿。
- 商品图 product-xiaoziping.jpg 已存在（64KB），init 种子 images 已配本地路径。清单第3节从"images:[]"更正为"已有图"。
- 上线 6 步操作路线图已产出（重部署云函数→真机验证支付→填隐私指引→选类目→上传代码→提审）。全部为用户控制台操作，代码侧已无阻塞项。

## 订单详情页白屏 · 第二轮排查
- 用户反馈"还是不能正常显示"，上次移除 `__usePrivacyCheck__` 未解决。
- 全面重写 order-detail 三件套（js/wxml/wxss/json）：
  - JS：全链路 console.log 日志追踪（onLoad/loadOrder/setData 每一步）；API 返回值 JSON 输出；异常捕获输出 stack；防重入守卫保留。
  - WXML：顶部固定调试状态条（黑底绿字，z-index:9999）永远可见，显示当前状态（加载中/错误/order=null/已加载）；取消 skeleton/error-state 组件依赖，全部改为内联 HTML（消除组件加载失败的可能）；空 order、空 items、无 address 均有 fallback。
  - WXSS：补 debug-state 样式、err-wrap/err-icon/err-text/err-btn 内联错误态样式；status-bar 渐变改用 --color-primary-soft（中绿）替代 --color-primary-light（杏色，色相跳变太大）。
  - JSON：移除不再使用的 skeleton/error-state 组件注册。
- 修复 retry() 因 this.data.order 为 null 时无法重试的 bug（改用 this._orderId）。
## 订单列表页故障修复（第二轮）
- 用户反馈"还是不行"，根因：上一轮新增 `myRefunds` action 需要手动部署云函数，用户未部署/部署失败导致接口 404。
- 修复策略：**移除 `myRefunds` 依赖**，所有 tab 统一走 `getOrderList`（已有 `list` action 支持任意 status 过滤），无需额外部署云函数。
  - order.js: `refunding` tab 从 `API.getMyRefunds()` 改回 `API.getOrderList({ status: 'refunding' })`
  - user.js: `loadOrderCounts` 中 `API.getMyRefunds` 改回 `API.getOrderList({ status: 'refunding', pageSize: 1 })`
  - order.wxml: 修复 `wx:key="productId-{{item.spec || ''}}"` → `wx:key="productId"`（wx:key 不支持模板语法）
  - checkout.js: `fail: () => wx.switchTab({ url: '/pages/order/order' })` → `wx.navigateTo`（order 不是 tabBar 页）
- goDetail 统一跳 order-detail（不再区分 refunding tab 跳 refund 页，因为订单列表里是订单 ID 不是退款 ID）
- ✅ 用户确认"可以了"——订单列表页全部正常（待付款/待发货/待收货/退款售后四个入口均正常显示）。
- 当前上线进度：第1步✅商户绑定+真实支付 / 第2步✅真机支付0.01元验证 / 第3步进行中：小程序后台已完成信息+类目+认证，待完成备案+隐私指引 / 第4-5步待做：上传代码+提审。
- 待办：提审前需移除 order-detail 和 order 页面的调试状态条（黑底绿字 debug bar）。

## 代理加入页权益展示调整
- 初版：固定 15% 佣金 → 改为三档递增 15%/20%/25% → 用户要求不暴露具体比例，再改。
- 最终版：移除所有佣金数字和阶梯区块，改为四宫格权益图标（永久用户绑定/阶梯返利成长/快速结算到账/专属客服支持）+ "如何赚钱"三步引导（分享商品→好友下单→获得返利）。
- 不在界面体现任何具体佣金比例，后端逻辑不变（admin_config.commissionRate 控制）。
